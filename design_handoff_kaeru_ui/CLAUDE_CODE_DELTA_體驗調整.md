# 追加改動：體驗調整（2b）

貼給 Claude Code。設計稿 `Kaeru 產品畫面.dc.html` 的 `2b` 區塊，截圖 `screenshots/44`–`48`，另外 `38-empty-unnamed.png` 與 `08-scenario.png` 有文案改動。

五件事，都是把高頻或高風險的動作拉近。顏色用既有色票，不要新增，不要套 Whoscall 品牌色。

## 1. 選單改成右上角彈出的浮層（`44-menu-popup.png`）

現在的選單從畫面底部長出來（bottom sheet），改成**從標題列 `≡` 錨定、往下彈出的浮層**：

- 底層畫面 `opacity:.28`，遮罩 `rgba(73,70,64,.22)`（比 sheet 淡，浮層本身有陰影撐出層次）
- 浮層 `position:absolute; right:14px; top:62px; width:262px`，白底、**`border-radius:14px`**、`overflow:hidden`、`box-shadow:0 20px 44px -14px rgba(73,70,64,.42), 0 0 0 1px rgba(73,70,64,.06)`
- 貼近右上角的 `≡`，看得出是從那顆按鈕長出來的。**不要置中、不要貼齊底部、不做四方角** — 這是全 App 唯一用圓角的容器，因為它是浮在畫面之上的層，不是版面的一部分
- 進場動畫：`opacity 0→1` + `scale(0.96)→1`，`transform-origin: top right`，180ms

內容自上而下：

1. **「＋ 新增收據」實心主按鈕** — 外層 `padding:14px`，按鈕 `background:#77899A`、`border-radius:10px`、`padding:13px`、置中、白字 14.5/700。這是 App 裡最高頻的動作，放在拇指最先到的位置
2. 六列導覽（列間 `1px solid #E1DCD5`、`padding:13px 18px`、`gap:14px`）：**總覽**（倒數、金額、回程當天流程）／**收據**（新增與管理每一張收據）／**查驗**（機場現場核對還剩幾張）／**FQA**（規則、小撇步、試走一趟）／**設定**（匯率、語言、資料）／**切換行程**（大阪 11 月 · 進行中，右側 `›`）
   - 每列左側 21px 線性圖示（`stroke-width:1.6`、`stroke:#8B857D`、24 格），目前那頁的圖示轉 `#5C6D7C`
   - 標題 15px、副標 11.5px `sub`；目前那頁整列 `background:#DFE5EA`、標題 700 `blueDeep`
3. 最後一列 **語言**：左圖示 + 「語言」15px，右側顯示目前值（`日本語`）12.5–13px `blueDeep` 600。直接在這裡切，不用進設定

「查驗進度」在選單裡簡稱**查驗**（浮層寬度 262px，兩字比較穩），頁面標題仍是「查驗進度」。

**行程切換面板維持 bottom sheet**，兩者不要混：選單是導覽（浮層），行程切換是設定某個值（從底部長出來）。

## 2. 總覽在回程當天長出行動列（`45-home-today.png`）

**只在回程班機起飛前 24 小時內出現**，其他時候完全不存在 — 不要用 disabled 或灰掉的版本佔位。

倒數下方插入一條灰藍實心列（`background:#77899A`、`padding:15px 16px`）：

- 左：`今天要辦的事` 10px/700 `#DFE5EA` `letter-spacing:0.2em` → `還有 3 張沒辦完` 15/700 白字 → 金額 11.5px `#DFE5EA` tabular
- 右：`去核對 ›` 12.5/700 白字，`flex:none`
- 點了直接進查驗進度

全部辦完後這條換成灰綠（`#93A392`）、文字改「都辦完了」，不再是行動列（拿掉 `›`）。

倒數在 24 小時內建議改成「小時 + 分」（`14 小時 22 分`），不要再顯示天數。

## 3. 期限警示（`45-home-today.png`、`47-receipts-pending.png`）

三個層次，講金額不要只講天數：

| 剩餘天數 | 位置 | 樣式 |
|---|---|---|
| ≤30 天 | 收據卡 | 灰赭**填色**標籤 `期限剩 14 天`（原本是純文字） |
| ≤14 天 | 總覽 | 灰赭實心列（`#B08D74`）：`1 張收據 14 天後過期` 13/700 白字 + `¥13,000 拿不回來 · ヨドバシ梅田` 11.5px `#F3E7DD` |
| ≤3 天 | 推播 | 一次，不要每天 |

**過期的收據不要刪也不要隱藏。** 改成失效樣式，跟「已在境內消費」同一套：卡底 `#F4F2EF`、金額 `sub` 色 + `line-through`（劃線色 clay）、群組表頭掛 `已過期` clay 填色標籤、卡內 11px `clayInk` 寫「已超過 90 天期限」。使用者要看得到自己錯過了什麼。

## 4. 快速新增：先拍照，晚點補（`46-quick-add.png`）

現在只有一條路徑（完整表單），在店門口站著填不完。加一條快路，完整表單保留。

拍完照 → OCR 帶出金額與稅率 → 存。畫面：

- 標題列：取消／「拍好了」／存起來
- 照片預覽 196px + 標籤 `讀到金額了`（sage）+ 線框壓縮前後大小
- `#F4F2EF` 區塊：「從照片讀到的含稅金額」+ 副標「稅率 10% · 稅抜 ¥3,400 · 稅額 ¥340」，右側金額 24/600；下方 `1px solid #E1DCD5` 後達標狀態（`未達 5,000 円 · 還差 ¥1,600`，`clayInk` 600）
- **退款方式改成必答三選一**：`有登記`／`沒有`／`不確定`，各 `flex:1`，選中 blue 實心。下方 11.5px `sub`：「最容易漏掉的一步。沒登記的話，錢不會自動退。」— 這一段是四段狀態裡最容易漏、漏了最貴的，不能再只是個開關
- 讀不到的欄位（店名、日期）列出來掛 `待補` 線框標籤，不要用空白輸入框假裝
- 主 CTA「存起來，晚點再補」；下方次要「現在就填完整資料」

## 5. 待補狀態與退款確認

**待補收據（`47-receipts-pending.png`）**

- 卡框改 `1px dashed #E1DCD5`（虛線＝資料未完成，跟空狀態的待填容器同一個語意），群組表頭底線也是虛線
- 店名位置寫「店名待補」`sub` 色，日期寫「11/12 拍的」
- 掛 `資料待補` 線框標籤，卡內底部「補上店名和日期 ›」12px `blueDeep` 600
- 篩選 tab 加一個「待補」
- 清單頂端 `#F4F2EF` 提示：「2 張資料待補」+「待補的收據不算進預估可退稅額。有空時補上店名和日期。」
- **待補的收據不計入總覽的預估可退稅額** — 金額可能是 OCR 誤讀，不能拿去算承諾

**退款確認（`48-refund-check.png`）**

不要求使用者每天回來記「已退款」。改成**回程後 7 天推一次通知**：「大阪 11 月 的 3 張收據，錢進來了嗎？」點開是這一頁：

- 標題列：‹ 返回／「錢進來了嗎」／完成
- 段落標籤「大阪 11 月 · 回程後 7 天」+ 說明 14px/line-height 1.95
- `1px solid #494640` 後「還在等」+ 金額 26/600 `blueDeep`
- 每張一列（列間 `1px solid #E1DCD5`）：店名 14.5/700 + 「11/08 · 稅額 ¥240」11px tabular，右側 22×22 勾選框（已勾 `#93A392` 白勾、未勾 `1px solid #E1DCD5`）
- `#F4F2EF` 說明：入帳時間各店不同，兩到三週正常，超過一個月先找店家不是海關
- 底部兩顆：`還沒收到`（線框）／`都收到了`（實心 blue，`flex:1.4`）
- 最下面 11px「7 天後再提醒我一次」

## 6. 情境模擬搬家與改名（`38-empty-unnamed.png`、`08-scenario.png`）

- 標題「情境模擬：一趟大阪之旅」→ **「試走一趟大阪」**
- 副標「六個決定，看你最後能退多少」→ **「跟著走一趟，看你能退多少」**
- 進度指示器維持「3 ／ 9」（拿掉「決定」二字）
- **空狀態（未命名）那頁的三條規則摘要換成情境模擬入口**：段落標籤「還沒搞懂規則？」+ `#F4F2EF` 區塊（標題 18/700、說明「跟著走一趟，看你能退多少。門檻、失效、期限，走完就懂了。」、`開始這趟旅程 ›`）。第一次打開 App 的人最需要它，而那時他還沒有收據可看
- FQA 裡保留入口，但不再是主要位置
