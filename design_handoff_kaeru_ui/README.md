# Handoff: Kaeru 日本退稅小幫手 — 視覺方向 2a

## Overview

Kaeru 是 2026 年 11 月日本消費稅免稅新制（先付後退）的旅客小幫手 App。使用者把每張免稅收據集中管理，追蹤四段處理狀態、90 天期限、同店同日稅抜 5,000 円門檻，出境前在機場用「查驗進度」頁自己核對還剩哪幾張沒辦。

視覺方向已定案為 **2a**，設計稿 `Kaeru 產品畫面.dc.html` 只包含這個方向，全部畫面都要實作。（第一輪的 1a / 1b / 1c 已從設計稿移除。）

## About the Design Files

這個 bundle 裡的 HTML 檔案是 **設計參考稿**：用 HTML 畫出來的視覺與行為意圖，不是可以直接搬進產品的程式碼。

任務是把這些畫面 **在目標 codebase 既有的環境裡重做一次**（此專案已有可跑的 React 原型 `kaeru.jsx`，請沿用它的元件結構、state 與 `window.storage` 持久化方式，只換掉視覺層）。原型中的商業邏輯（`netOf`、`daysLeft`、`groups`、`taxOf`、`stats`、SIM 題庫、QA/TIPS/RULES 文案）已經正確，**不要重寫，只改 render**。

## Fidelity

**High-fidelity。** 顏色、字級、字重、間距、邊框都是最終值，請照數值實作。手機寬度基準 390px（iPhone 14/15 邏輯寬度），高度 844px。

## Design Tokens

### 色彩（全部為最終值，不要另生色階，不要套用 Whoscall 品牌色）

| Token | Hex | 用途 |
|---|---|---|
| `page` | `#FFFFFF` | 畫面底色 |
| `outside` | `#F2F0ED` | App 外圍／桌面背景 |
| `fill` | `#F4F2EF` | 填色區塊（稅額計算列、情境模擬卡、失效收據底色、照片框） |
| `blue` | `#77899A` | 主色。主要按鈕、進行中標籤、序號、段落標籤、進度已完成點 |
| `blueDeep` | `#5C6D7C` | 深灰藍。金額強調（稅額、可退稅額）、次要文字連結、wordmark |
| `blueSoft` | `#DFE5EA` | 淺灰藍。開關開啟時底色 |
| `sage` | `#93A392` | 灰綠。達標標籤、查驗進度條已完成段、青蛙標誌 |
| `clay` | `#B08D74` | 灰赭。警示：未達標、已在境內消費、期限逼近 |
| `clayInk` | `#8A6448` | 灰赭深色文字（僅用於文字，不做填色標籤底） |
| `ink` | `#494640` | 主文字、重要分隔線 |
| `sub` | `#8B857D` | 次要文字、標籤說明 |
| `line` | `#E1DCD5` | 分隔線、線框標籤、未完成進度點 |

顏色語意規則（重要，不可混用）：

- 灰綠 = 已達標／已完成
- 灰赭 = 警示，分兩種語氣：**還救得回來**（未達 5,000 円、期限剩幾天）與**已失效**（已在境內消費）
- 灰藍 = 進行中、還有下一步
- 線框標籤（`1px solid #E1DCD5` + `#8B857D` 文字）= 純紀錄，不搶注意力（已拆封、期限剩 62 天）

### 字體

```
font-family: "Noto Sans TC", "Noto Sans JP", "Hiragino Sans", "Yu Gothic",
             "Microsoft JhengHei", system-ui, -apple-system, sans-serif;
```

日文介面把 `"Noto Sans JP"` 提到最前。**不使用襯線體**（中日混排會不一致）。權重只用 500 / 600 / 700。

### 字級

| 用途 | size / weight | 備註 |
|---|---|---|
| 主數字（倒數、還剩張數） | 48–52 / 600 | `font-variant-numeric: tabular-nums`、`letter-spacing:-0.01em` |
| 可退稅額（總覽主角） | 42 / 600 | `blueDeep` |
| 頁面標題 | 20–30 / 700 | |
| 店名（清單／進度） | 13.5–20 / 700 | |
| 金額（一般） | 18–26 / 600 | 一律 tabular-nums |
| 內文 | 13 / 400，line-height 1.75–2 | |
| 次要說明 | 11–12.5 / 400，`sub` | |
| 段落標籤 | 10.5 / 700，`letter-spacing:0.22–0.26em`，`blue` | 全部欄位標題都用這個 |
| 填色標籤 | 10 / 600，白字，`padding:3px 7px`，`border-radius:3px` | |

**所有數字都要 tabular-nums**，金額不用細體（原本 1c 的 300 已經廢棄）。

### 形狀與間距

- 手機外框 `border-radius:30px`，`1px solid #E1DCD5`
- 頁面左右內距 26px（收據清單同）
- **收據與收據卡三態**：四方角（radius 0）、`1px solid #E1DCD5`、群組底線 `1px solid #494640`、同群組第二張以 `1px dashed #E1DCD5` 分隔 → 這是「票券語彙」，**只用在收據清單與收據卡**
- 其他所有頁面：無卡片、無圓角，只用 `1px solid #E1DCD5` 分隔線與留白（`#494640` 用於區塊起始的重線）
- 填色標籤 radius 3px；主要按鈕四方角，`padding:13–14px`，`background:#77899A`，白字 700

## Navigation

**沒有底部分頁列。** 導覽全在標題列右側：`＋`（新增收據）、`▤`（收據清單捷徑，在收據頁時轉 `#77899A`）、1px 分隔、`≡`（選單：切換行程／語言／各頁）。標題列底部 `1px solid #E1DCD5`，左側是 wordmark（`KAERU`，12.5px/600，`letter-spacing:0.28em`，`blueDeep`）＋青蛙標誌 26px＋行程名，或該頁標題（20px/700）。

`▤`／`＋`／`≡` 是 placeholder 字元，實作請換成 24px grid、2px stroke 的圓角線性 icon（原型用 lucide：`Plus` / `Receipt` / `Menu` / `X` / `ChevronRight`）。

## Screens

### 1. 總覽（Home）

**用途**：出發倒數與金額總結，落地後第一眼。

版面自上而下：
1. 標題列（如上）
2. 倒數區：段落標籤「距離班機起飛　出発便まで」→ 52px/600 `2 天 14 小時`（單位 16px/500，左右 margin 12px/4px）→ 兩顆填色標籤：`建議 11/14 12:00 抵達`（blue）、`託運前先辦查驗`（clay）
3. `1px solid #494640` 重線
4. 四列數字，列間 `1px solid #E1DCD5`，label 左 12.5px `sub`／數字右：
   - 含稅消費合計 24/600 ink
   - 預估可退稅額 **42/600 blueDeep**，label 下方接 `≈ NT$3,264` 11px
   - 還沒處理 24/600 + `進行中` 標籤（blue）
   - 最近到期 24/600 + `剩 12 天` 標籤（clay）
5. `1px solid #494640` + 段落標籤「出發當天流程」+ 編號清單（`01` 11px/700 blue、gap 14px、內文 13px/line-height 1.75）

倒數為 0 或未設定出發時間時，倒數區換成「設定出發時間」入口（沿用原型 `HomeView` 的判斷）。

### 2. 收據清單（Receipts）

**用途**：新增與管理每一張收據；三種狀態要能並排比較 —— 這頁保留票券語彙。

- 篩選：底線式 tab（全部／待處理／未達標／已完成），選中 12.5px/700 ink + `2px solid #494640` 底線
- 依「同一間店＋同一天」分組（`groupKey`）。群組表頭：左店名 13.5/700＋`2026-11-08 · 2 張` 11px；右 `稅抜合計 ¥6,200` 11px＋達標標籤。群組底線 `1px solid #494640`；**未達標時底線換 `#B08D74`**
- 群組內每張收據：`1px solid #E1DCD5`（頂邊由群組線接續，第二張起頂邊 `1px dashed`），內距 12px 14px
  - 第一行：含稅金額 18/600 左，`稅額 ¥240 ≈ NT$50` 11px 右
  - 四段進度：4 個 7×7px 方點以 1px 線相連，已完成 `#77899A`、未完成 `#E1DCD5`；下方 9.5px 左「目前狀態」（blueDeep/600）、右「下一步 …」
  - 標籤列：填色標籤（警示）＋線框標籤（紀錄）

### 3. 查驗進度（Progress）— 原「查驗模式」

**用途已改**：這頁**不是給海關看的**。海關看機台判定與實體商品。這頁是使用者在機場排隊時自己核對進度：還剩哪幾張沒辦、辦完一張消掉一張。

- 版面用分隔線語彙（**不要票券框、不要虛線**）
- 頂端：段落標籤「機場現場自己對　還沒辦完的收據」→ `還剩 3 張` **48/600 tabular**（單位 16px/500），右側金額 18/600 blueDeep
- 進度條：`height:1px` 的兩段 flex，完成部分 `#93A392`、剩餘 `#E1DCD5`；下方 11.5px 左「已辦完 2 ／ 5 張」右「已退回 ¥2,304」
- 每筆（列間 `1px solid #E1DCD5`，第一筆上方 `#494640`，內距 10px 0）：
  - `01` 12px/700 blue 左、日期 12px `sub` 右
  - 店名 20/700
  - 含稅／稅額並排（gap 30px）：label 11px `sub`，數字 18/600，稅額用 `blueDeep`
  - 備註（若有）12px `sub`
  - 線框按鈕「辦完了」：`1px solid #77899A`、`#5C6D7C` 12.5/600、置中、`padding:9px`
- 底部：`3 張都辦完了` 實心 blue 按鈕，**僅在剩 2 張以上時顯示**；下方 11px 註記「查驗以一張收據為單位，商品要全部帶在身上」
- **標籤不做中日並列**（原本「税込 / 含稅」是為海關而設，現已不需要）；跟隨介面語言

行為：按單筆「辦完了」→ 該筆 `status` 進到 `verified`、從清單移除、剩餘張數與進度條前進（動畫 180–220ms `cubic-bezier(0.4,0,0.2,1)`）。剩 1 張時隱藏批次按鈕。全部辦完顯示完成狀態（沿用原型 `checkEmpty` 文案）。

### 4. FQA

- 標題 20/700；分區段落標籤（購買時／出境時／退款／小撇步），前綴 22×1px blue 短線
- 問答列：`1px solid #E1DCD5` 上邊，`padding:15px 0`，問題 15px/line-height 1.7，右側 `›`（展開時 rotate 90deg）；答案 13px/line-height 2 `sub`，`margin:12px 24px 0 0`
- 小撇步：`01` 12px/700 blue opacity .7＋內文 13px/line-height 2，項間 22px
- 情境模擬入口：`#F4F2EF` 填色區塊，padding 22px，標題 18/700、副標 12.5px `sub`、`開始這趟旅程 ›` 13px/600 blueDeep

### 5. 設定

各列 `padding:18px 0`，列間 `1px solid #E1DCD5`（第一列上方 `#494640`）：行程（值 17px＋`進行中` 標籤＋張數）、出發時間（17/600 tabular）、匯率（17/600＋「抓即時匯率」底線連結＋更新時間 11px）、語言（兩個 15px 選項，選中 700＋`2px solid #494640` 底線）、資料說明＋「清空所有資料」（`clayInk` 文字＋`1px solid #B08D74` 底線，不做填色按鈕）。頁尾「規則依據：観光庁 消費税免税店サイト」10.5px 置中。

### 6. 新增收據（Sheet）

標題列：取消／新增收據（15/700）／儲存（13/700 blueDeep），底線 `#494640`。欄位皆為**底線式輸入**（focus 時底線 `#494640`），label 用段落標籤：

- 店名（16px）、購買日期（16/600 tabular＋日曆 icon）
- 含稅金額（20/600）
- **稅率：三選一**，見下方「稅率欄位」一節
- 稅額列：`#F4F2EF` 區塊，左「稅額（自動計算，可手動改）」＋「稅抜合計 ¥3,800」，右 22/600 blueDeep
- **稅抜合計後面接達標狀態**（10.5px/600、`margin-left:7px`，跟著輸入即時更新）：未滿 5,000 円 → `#B08D74`「未達 5,000 円 · 還差 ¥407」（差額 = `5000 - 稅抜合計`）；滿 5,000 円（含等於）→ `#93A392`「已達 5,000 円」。門檻是稅抜不是含稅 — 含稅 4,979 回推稅抜只有 4,593，其實不能退。混合稅率時用兩格的稅抜合計判定。只加這一行，不顯示同店同日合計、不加補買提示、不新增區塊（`25`–`27`）
- 三個開關：已登記退款方式（開＝`#DFE5EA` 底＋`1px solid #77899A`）、已拆封（線框，副標「純記錄，不影響退稅」）、已在境內吃掉或用掉（`1px solid #B08D74`，副標「整張收據都會無法退稅」）。開關本體 34×18px 四方角
- 收據照片：`1px dashed #E1DCD5`，高 74px，置中「拍照或選檔」— 點下去的完整流程見 `CLAUDE_CODE_DELTA_收據照片.md`（來源面板、相機、相簿多選、裁切與 OCR 帶入；截圖 `28`–`31`）
- 備註：底線式

### 6a. 稅率欄位（新增收據內）

**為什麼改**：日本同一張收據常常同時有 8% 和 10%。食品是輕減稅率 8%，酒類和其他商品 10%，外食內用 10%、外帶 8%。只能選一個稅率時，混合情況算出來的稅額會錯。

**三題按鈕**：`8%` ｜ `10%` ｜ `兩種都有`，`flex: 1 / 1 / 1.5`、`gap:6px`、`padding:10px 0`、13px/600。選中 `#77899A` 底白字，未選 `#F4F2EF` 底＋`1px solid #E1DCD5`＋`sub` 文字。四方角。日文標籤：`8%` / `10%` / `両方あり`。

按鈕下方固定一行說明（11.5px/line-height 1.75、`sub`）：「以收據上的標示為準，有 ※ 記號的是 8%。」日文：「レシートの表示を優先。※ マークは 8% 対象。」

**單一稅率（`19-tax-single.png`）**：點 `8%` 或 `10%` 就結束，下方維持現有的「含稅金額」單一欄位＋稅額區塊。九成的情況是單一稅率，不要為了少數情況拖慢主流程。

**兩種都有（`20-tax-mixed.png`）**：點第三顆才展開。原本的單一「含稅金額」欄位 **消失**，換成 `1px solid #E1DCD5` 上邊後的兩格（列間 15px）：

- 左側 label：`8% 對象` 12.5/700 ink＋副標 `食品、外帶` 10.5px `sub`
- 左側 label：`10% 對象` 12.5/700 ink＋副標 `酒類、其他商品`
- 右側輸入：`flex:1; max-width:168px`、`text-align:right`、19/600 tabular、底線 `1px solid #E1DCD5`（focus 時 `#494640`）；placeholder 文字為「含稅金額」`sub` 色

**為什麼是輸入含稅對象金額而不是稅額**：日本收據下方本來就印成「8%対象 4,104円（内消費税 304円）」，欄位對得起來，眼睛不用在收據上跳來跳去。

兩格下方的 `#F4F2EF` 匯總區塊（padding 14px）：

1. 「含稅總金額」＋「自動帶入」11px `sub` 左，20/600 ink 右 — **使用者不用再填一次總額**
2. `1px solid #E1DCD5` 後兩行 11.5px `sub` tabular：`8% 對象 稅抜 ¥3,800` / `稅額 ¥304`；`10% 對象 稅抜 ¥8,000` / `稅額 ¥800`
3. `1px solid #E1DCD5` 後稅額合計列：左「稅額合計（可手動改）」＋「稅抜合計 ¥11,800」，右 22/600 blueDeep

計算：`tax8 = round(v8 / 1.08 * 0.08)`、`tax10 = round(v10 / 1.10 * 0.10)`，達標判定用**稅抜合計**（`v8/1.08 + v10/1.10`）對 5,000 円門檻，不是含稅總額。

**只填一格（`21-tax-mixed-partial.png`）**：匯總區塊的含稅總額改 `sub` 色（尚未完成），已填那行照常顯示，未填那行右側寫「還沒填」；下方一顆 clay 填色標籤 `10% 對象還沒填`＋一行 11.5px `sub` 提示「照收據上的兩行金額各填一格。這張只有 8% 的話，回上面選 8% 就好。」儲存按鈕不鎖，但儲存時若仍只填一格，自動降回該單一稅率模式。

**切換行為**：從混合切回單一時，兩格的值相加帶回單一「含稅金額」，兩格不清空（使用者切回來時還在）。單一模式下兩格完全不出現。展開動畫 180ms。

### 7. 收據詳情（Sheet）— 三態


- 標題列：`‹ 收據`／編輯（blueDeep）＋刪除（clayInk）
- 店名 19/700＋`2026-11-08 · 期限剩 62 天` 11.5px
- `1px solid #494640` 後：含稅（26/600 ink）與稅額（26/600 blueDeep）左右並排；下一列 11.5px 三欄（稅率／稅抜金額／同店同日合計）
- 標籤列（達標／待查驗／已拆封）
- 目前狀態：四段可點的列，每列 9×9px 方塊（已達 `#77899A` 實心、未達 `1px solid #E1DCD5` 空心）＋狀態名＋右側日期或「標記 ›」；**目前那段** `#F4F2EF` 底、名稱 700、右側 `目前` 標籤（blue）
- 說明區塊 `#F4F2EF`：拆封／穿過不影響，消費才失效（文案沿用原型 `packNote`）
- 收據照片 96px `#F4F2EF` 框

**已在境內消費**（`15-detail-consumed.png`）：含稅與稅額都改 `sub` 色 + `line-through`（劃線色 clay），稅額 label 換成「稅額 拿不回來」；標籤列 `已在境內消費`＋`整張失效`（皆 clay）；狀態列第二段換成 **流程中止**（方塊 `1px solid #B08D74` 空心、文字 `clayInk` 700、右側 `不能退` clay 標籤），後兩段右側顯示 `—` 不給「標記」動作；說明區塊 `#F4F2EF` + **`border-left:3px solid #B08D74`**、文字 `clayInk`（文案用原型 `warnConsumed`）；下方再接一行 12px `sub` 補「拆封／穿過不算消費」。

**已退款**（`16-detail-refunded.png`）：四段狀態方塊全部 `#93A392` 實心、各段右側帶日期，最後一段 `#F4F2EF` 底 + `完成`（sage）標籤；標籤列 `已退款`（sage）＋線框 `信用卡退回`；稅額 label 改「已退回 ≈ NT$420」；說明區塊 `#F4F2EF`、`ink` 文字（退款由店家或委託業者處理、對不上先找店家）。

### 8a. 行程切換（Bottom Sheet，`17-trip-switch.png`）

底層畫面 `opacity:.35`，上方遮罩 `rgba(73,70,64,.28)`，面板貼底、`border-top:1px solid #494640`、`padding:22px 26px 30px`，**不做圓角**。內容：標題「行程」18/700＋`✕`；目前行程（17/700＋`進行中` blue 標籤＋`4 張收據 · 出發 2026/11/14 15:00` 11.5px＋線框「刪除這趟行程」，刪除前用原型 `tripDeleteConfirm` 二次確認）；段落標籤「過去的行程」，每列店名 15px＋張數日期 11.5px，右側「切換到這趟」12.5px `blueDeep` 底線；段落標籤「新增行程」，兩個底線式欄位（行程名稱、選日期和時間）＋實心 blue「建立」。

### 8. 情境模擬

- 標題列：`‹ 回到 FQA`／`決定 3 ／ 9`
- 九段進度條（3px 高、gap 3px、完成 blue）
- `DAY 1 · 結帳櫃檯` 段落標籤 左；右「你退到 ¥15,240」（金額 17/600 blueDeep，即時累加，沿用原型 `useCountUp`）
- 情境 14px/line-height 2；問題 19/700
- 選項：未選 `1px solid #E1DCD5` + `sub` 文字；選中 `1px solid #93A392` + `#F4F2EF` 底，展開回饋 12.5px/line-height 1.95，右上角標籤「這步做對了」（sage）或「這步有問題」（clay），下方損失標籤（clay）
- 底部：`這趟最多可退 ¥15,544` 11px 左，`繼續 ›` 實心 blue 按鈕右

**結算頁**（`18-scenario-result.png`）：段落標籤「這趟走完了」→ 左「你退到」＋ **48/600 blueDeep** 金額（count-up），右「最多可退 ¥15,544」（17/600 ink）；1px 進度條，退成功比例 `#93A392`、漏掉部分 **`#B08D74`**；下方 11.5px 左「退成功 8 ／ 9 個決定」右「漏掉 ¥304」（clayInk 600）。接著段落標籤「漏掉的原因」：每筆為 `決定 03`（12px/700 blue）＋`損失 ¥304`（clay 標籤）＋選項標題 15/700＋回饋 12.5px/line-height 1.95；全對時改用原型 `simPerfect` 文案。最後段落標籤「規則整理」（原型 `RULES` 前三條，編號 blue）＋兩顆等寬按鈕：`再走一次`（線框 blue）／`回到 FQA`（實心 blue）。

### 9. 日文介面

同版面換 `Noto Sans JP`。字串取原型 `T.ja`。已驗證寬度的長字串：`空港到着 11/14 12:00`、`預ける前に税関確認`、`レシート全体が対象外`、`税関確認はレシート単位。商品はすべて所持してください`。單位換成 日／時間／件。查驗頁標題「手続きの進捗」，按鈕「手続きした」「3 件すべて完了にする」。

## 收據卡三態（最重要）

這個 App 最容易出事的地方是狀態長太像。三態必須能並排辨識：

| 狀態 | 群組底線 | 卡底色 | 金額 | 進度點 | 標籤 |
|---|---|---|---|---|---|
| 正常待查驗 | `#494640` | 白 | 18/600 ink | 前兩點 blue 實心 | `待查驗`(blue)＋線框 `已拆封`/`期限剩 62 天` |
| 未達 5,000 円 | **`#B08D74`** | 白 | 18/600 ink | 第一點 **clay**，其餘空 | `還差 ¥1,600`(clay，群組)＋`未達 5,000 円`(clay)＋線框 `當天回同店可補買` |
| 已在境內消費 | `#494640`（群組仍達標） | **`#F4F2EF`** | **`sub` 色＋`line-through`（劃線色 clay）** | 第一點 clay，其餘 **空心方框** | `已在境內消費`＋`整張失效`（皆 clay）＋卡內 11.5px clayInk 說明 |

失效狀態改變的是**卡本身**（底色、劃線、空心點），不只換標籤文字。

## RWD

單欄置中，不切兩欄、不加側邊導覽，導覽永遠在標題列。

### 容器

```css
.app {                        /* 內容欄 */
  width: 100%;
  max-width: 480px;
  margin-inline: auto;
  background: #FFFFFF;
  min-height: 100dvh;
}
body { background: #F2F0ED; } /* 欄外留白 */
```

- 內容欄從 320 長到 **480 就停住**，之後只有左右留白變多。桌機不需要另一套版面。
- ≥768 時欄的上下貼滿視窗（`min-height:100dvh`），左右邊界可加 `1px solid #E1DCD5`，不要圓角、不要陰影，不要做成漂浮卡片。
- 手機不要 `border-radius`（設計稿上的 30px 圓角只是手機外框示意，不是 UI）。

### 斷點

| 寬度 | 行為 |
|---|---|
| < 390 | **整體等比縮放**，版面不重排：`html { font-size: calc(16px * min(1, 100vw / 390)); }`，或在 `.app` 上 `transform: scale(100vw / 390)` + `transform-origin: top center`。字級、間距全部改用 `rem` 才會跟著縮 |
| 390–480 | 欄寬 = 視窗寬，內距 26px。基準設計尺寸 |
| 480–767 | 欄寬鎖 480，內距 26px，兩側留白 |
| ≥ 768 | 同上，欄不再變寬，留白吃掉其餘空間；標題列與底部按鈕都在欄內，不做 full-bleed |

### 隨寬度變的值（只有這些，其他一律不動）

| 屬性 | <390 | 390–480 | ≥480 |
|---|---|---|---|
| 左右內距 | 26px（隨縮放） | 26px | 30px |
| 主數字（倒數／還剩張數） | 48px 隨縮放 | 48px | 52px |
| 可退稅額 | 42px 隨縮放 | 42px | 46px |
| 內文 | 13px 隨縮放 | 13px | 13px（不放大） |
| 收據卡群組間距 | 18px | 18px | 22px |

字級只在主數字放大，內文與標籤不隨寬度變。

### 各畫面的注意事項

- **總覽**：四列數字是 label 左／值右的 `space-between`，寬度變化只影響中間空白。倒數的「2 天 14 小時」是一行，`white-space: nowrap`
- **收據清單**：群組表頭左右兩欄，右欄 `text-align:right` 且 `flex:none`；店名 `min-width:0` + `text-overflow:ellipsis`，長店名截斷而不是換行
- **查驗進度**：含稅／稅額 `gap:30px` 固定，不用 `flex:1` 撐開；1px 進度條寬度隨欄寬
- **FQA**：答案段落 `margin-right:24px` 保持右側呼吸，欄寬 480 時行長約 40 字，不再放寬
- **新增收據**：稅率 8%／10% 兩顆各 `flex:1`；三個開關列高度固定 44px 以上
- **詳情**：三欄摘要（稅率／稅抜金額／同店同日合計）在 <360 時允許換成兩行，其餘維持一行
- **情境模擬**：九段進度條用 `flex:1` 等分；選項卡寬度滿欄
- **行程切換**：底部面板固定在 **欄內**（`position:absolute` 相對 `.app`，不是 viewport），寬度跟著欄，≥768 時仍貼欄底，不改成置中 modal

### 其他

- `<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">`
- 安全區：標題列 `padding-top: max(14px, env(safe-area-inset-top))`；底部固定按鈕 `padding-bottom: max(22px, env(safe-area-inset-bottom))`
- 高度用 `dvh` 不用 `vh`（iOS Safari 網址列）
- 觸控目標最小 44×44，線框按鈕現為 `padding:9px` + 12.5px 字 ≈ 38px，實作時補到 44
- 橫向：手機橫放沿用同一單欄，不做橫式版面

### 設計稿對應

`Kaeru 產品畫面.dc.html` 最上方的 **RWD** 區塊：九個畫面的 430×932 版本，以及 768×1024 / 1024×768 / 1280×800 三個視窗示意（欄寬 480 置中、外圍 `#F2F0ED`）。390×844 版本在下方 2a 區塊。

## Interactions & Behavior

- Hover（桌機/平板）：線框按鈕底色 `rgba(73,70,64,0.04)`；實心 blue 按鈕壓深至 `#5C6D7C`
- Press：`transform:scale(0.98)`
- Transition：180–220ms `cubic-bezier(0.4,0,0.2,1)`；FAQ 展開 150ms
- 查驗進度：單筆「辦完了」→ 移除 + 進度條前進；批次按鈕僅剩 ≥2 張顯示
- 情境模擬：選項一經點選鎖定並展開回饋，金額 count-up 500ms
- 匯率：`open.er-api.com/v6/latest/JPY`，失敗顯示 `rateFail` 文案並允許手動輸入
- 資料只存本機（原型用 `window.storage`），照片先 `compressImage(maxSide 1000, quality .6)` 再存
- 期限：`daysLeft = 購買日 + 90 − 今天`；≤14 天用 clay，≤30 天用線框標籤，<0 顯示「已超過 90 天期限」

## State（沿用原型）

`items` / `photos` / `settings{rate, rateAt, lang}` / `trips` / `activeId` / `tab` / `editing` / `openId` / `quizOn` / `filter`。衍生值：`groups`（同店同日稅抜合計與是否達標）、`taxOf`、`stats{totalIncl, refundable, pendingCount, minDays}`。

## Assets

`icons/` 內是本次產出的圖示，可直接放進 `public/`：

| 檔案 | 用途 |
|---|---|
| `favicon.svg` | 現代瀏覽器 favicon（白底 + 灰綠青蛙，radius 5/24） |
| `favicon-16.png` / `-32.png` / `-48.png` | 舊版瀏覽器 |
| `apple-touch-icon-180.png` | iOS 加到主畫面 |
| `icon-192.png` / `icon-512.png` | PWA |
| `icon-512-maskable.png` | Android maskable（灰綠底、白青蛙、留 15% 安全邊） |
| `icon-mark.svg` | 純標誌，無底色，UI 內嵌用 |
| `icon-maskable.svg` | maskable 向量版 |
| `site.webmanifest` | theme_color `#93A392`、background `#FFFFFF` |

標誌是青蛙正臉、眼球突出頭部輪廓（幾何定義見 `icon-mark.svg` 的 path，24×24 座標系）。標題列用 26px、總覽 30px。

`<head>` 掛法：

```html
<link rel="icon" href="/favicon.svg" type="image/svg+xml">
<link rel="icon" href="/favicon-32.png" sizes="32x32">
<link rel="apple-touch-icon" href="/apple-touch-icon-180.png">
<link rel="manifest" href="/site.webmanifest">
<meta name="theme-color" content="#93A392">
```

沒有其他圖片素材。收據照片是使用者拍的，占位框用 `1px dashed #E1DCD5`。

## Files

| 檔案 | 說明 |
|---|---|
| `Kaeru 產品畫面.dc.html` | 設計稿（定案）。五個主畫面＋新增收據／詳情／情境模擬＋三個日文畫面＋補充畫面（詳情已消費／已退款、行程切換、情境模擬結算）＋收據卡三態 |
| `kaeru.jsx` | 既有 React 原型：商業邏輯、i18n 字串、QA/TIPS/RULES/SIM 資料。邏輯照用，視覺層替換 |
| `icons/` | 圖示與 manifest |
| `screenshots/` | 全部畫面截圖（2× PNG）：01 總覽、02 收據、03 查驗進度、04 FQA、05 設定、06 新增收據、07 詳情、08 情境模擬、09–11 日文三頁、12–14 收據卡三態、15 詳情已消費、16 詳情已退款、17 行程切換、18 情境模擬結算、19–21 稅率欄位三態、22–24 RWD、25–27 稅抜達標狀態、28–31 收據照片流程 |
| `CLAUDE_CODE_DELTA_收據照片.md` | 拍照與相簿流程的單頸指令 |
| `CLAUDE_CODE_DELTA_稅抜達標.md` | 這一項改動的單頸指令（若已照 README 實作則不需要） |
| `CLAUDE_CODE_PROMPT.md` | 複貼給 Claude Code 的開工指令 |

實作順序建議：先把 `kaeru.jsx` 的 `C` 色票換成上表、字體與字級規則統一，再逐頁替換 `HomeView` → `ListView` → `CheckView`（改為查驗進度）→ `FaqView` / `Scenario` → `SettingsView` → `EditSheet` / `DetailSheet`。
